export interface Provider {
  name: string;
  url: string;
}

export interface ApiService {
  name: string;
  description: string;
  swaggerUrl: string;
  contactEmail: string;
  contactName: string;
  contactUrl: string;
}

